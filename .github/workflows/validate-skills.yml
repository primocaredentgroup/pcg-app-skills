name: validate-skills

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate-skills:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate skill structure and public-safety checks
        run: python3 scripts/validate_skills.py

      - name: Verify native plugin matches canonical skills
        run: python3 scripts/sync_plugins.py --check

      - name: Validate staging plugin metadata and remote-only payload
        run: python3 scripts/validate_plugins.py
